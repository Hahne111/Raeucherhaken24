<div class="ad-shell">
  <aside class="ad-side">
    <div class="ad-brand">
      <strong>Räucherhaken24</strong>
      <span>Verwaltung</span>
    </div>
    <nav class="ad-nav" aria-label="Verwaltungsnavigation">
      <% if (adminCan('uebersicht')) { %><a href="/verwaltung/uebersicht" class="<%= currentPath === '/uebersicht' ? 'is-active' : '' %>"><svg aria-hidden="true"><use href="#i-star"/></svg>Übersicht</a><% } %>
      <% if (adminCan('produkte') || adminCan('kategorien') || adminCan('medien') || adminCan('lager.lesen') || adminCan('produkt.analyse')) { %><span class="ad-nav__sep">Katalog</span><% } %>
      <% if (adminCan('produkte')) { %><a href="/verwaltung/produkte" class="<%= currentPath.indexOf('/produkte') === 0 ? 'is-active' : '' %>"><svg aria-hidden="true"><use href="#i-package"/></svg>Produkte</a><% } %>
      <% if (adminCan('produkte')) { %><a href="/verwaltung/produkte/naturgewuerze" class="<%= currentPath === '/produkte/naturgewuerze' ? 'is-active' : '' %>"><svg aria-hidden="true"><use href="#i-leaf"/></svg>Naturgewürze</a><% } %>
      <% if (adminCan('produkt.analyse')) { %><a href="/verwaltung/auswertung/produkte" class="<%= currentPath.indexOf('/auswertung/produkte') === 0 ? 'is-active' : '' %>"><svg aria-hidden="true"><use href="#i-star"/></svg>Produktanalyse</a><% } %>
      <% if (adminCan('kategorien')) { %><a href="/verwaltung/kategorien" class="<%= currentPath.indexOf('/kategorien') === 0 ? 'is-active' : '' %>"><svg aria-hidden="true"><use href="#i-leaf"/></svg>Kategorien</a><% } %>
      <% if (adminCan('medien')) { %><a href="/verwaltung/medien" class="<%= currentPath.indexOf('/medien') === 0 ? 'is-active' : '' %>"><svg aria-hidden="true"><use href="#i-star"/></svg>Medien</a><% } %>
      <% if (adminCan('lager.lesen')) { %><a href="/verwaltung/lager" class="<%= currentPath === '/lager' || currentPath.indexOf('/lager/') === 0 ? 'is-active' : '' %>"><svg aria-hidden="true"><use href="#i-package"/></svg>Lager</a><% } %>
      <% if (adminCan('lager.lesen')) { %><a href="/verwaltung/lagerorte" class="<%= currentPath.indexOf('/lagerorte') === 0 ? 'is-active' : '' %>"><svg aria-hidden="true"><use href="#i-tools"/></svg>Lagerorte</a><% } %>
      <% if (adminCan('lager.lesen')) { %><a href="/verwaltung/inventur" class="<%= currentPath.indexOf('/inventur') === 0 ? 'is-active' : '' %>"><svg aria-hidden="true"><use href="#i-shield"/></svg>Inventur</a><% } %>
      <% if (adminCan('etiketten')) { %><a href="/verwaltung/etiketten" class="<%= currentPath.indexOf('/etiketten') === 0 ? 'is-active' : '' %>"><svg aria-hidden="true"><use href="#i-package"/></svg>Etikettenstudio</a><% } %>
      <% if (adminCan('finanzen')) { %><span class="ad-nav__sep">Finanzen</span>
      <a href="/verwaltung/finanzen" class="<%= currentPath === '/finanzen' ? 'is-active' : '' %>"><svg aria-hidden="true"><use href="#i-star"/></svg>Cockpit</a>
      <a href="/verwaltung/finanzen/eingangsbelege" class="<%= currentPath.indexOf('/finanzen/eingangsbelege') === 0 ? 'is-active' : '' %>"><svg aria-hidden="true"><use href="#i-package"/></svg>Eingangsbelege</a>
      <a href="/verwaltung/finanzen/bank" class="<%= currentPath.indexOf('/finanzen/bank') === 0 ? 'is-active' : '' %>"><svg aria-hidden="true"><use href="#i-shield"/></svg>Banking</a>
      <a href="/verwaltung/finanzen/offene-posten" class="<%= currentPath.indexOf('/finanzen/offene-posten') === 0 ? 'is-active' : '' %>"><svg aria-hidden="true"><use href="#i-cart"/></svg>Offene Posten</a>
      <a href="/verwaltung/finanzen/kreditoren" class="<%= currentPath.indexOf('/finanzen/kreditoren') === 0 ? 'is-active' : '' %>"><svg aria-hidden="true"><use href="#i-truck"/></svg>Kreditoren</a>
      <a href="/verwaltung/finanzen/anlagen" class="<%= currentPath.indexOf('/finanzen/anlagen') === 0 ? 'is-active' : '' %>"><svg aria-hidden="true"><use href="#i-tools"/></svg>Anlagen und Konten</a>
      <a href="/verwaltung/finanzen/planung" class="<%= currentPath.indexOf('/finanzen/planung') === 0 ? 'is-active' : '' %>"><svg aria-hidden="true"><use href="#i-star"/></svg>Planung</a>
      <a href="/verwaltung/finanzen/steuern" class="<%= currentPath.indexOf('/finanzen/steuern') === 0 ? 'is-active' : '' %>"><svg aria-hidden="true"><use href="#i-lock"/></svg>Steuern</a>
      <a href="/verwaltung/finanzen/abschluss" class="<%= currentPath.indexOf('/finanzen/abschluss') === 0 ? 'is-active' : '' %>"><svg aria-hidden="true"><use href="#i-shield"/></svg>Monatsabschluss</a><% } %>
      <% if (adminCan('kasse') || adminCan('kassenbuch')) { %><span class="ad-nav__sep">Kasse</span><% } %>
      <% if (adminCan('kasse')) { %><a href="/verwaltung/kasse" class="<%= currentPath.indexOf('/kasse') === 0 && currentPath.indexOf('/kassenbuch') !== 0 ? 'is-active' : '' %>"><svg aria-hidden="true"><use href="#i-cart"/></svg>Kasse</a><% } %>
      <% if (adminCan('kassenbuch')) { %><a href="/verwaltung/kassenbuch" class="<%= currentPath.indexOf('/kassenbuch') === 0 ? 'is-active' : '' %>"><svg aria-hidden="true"><use href="#i-shield"/></svg>Kassenbuch</a><% } %>
      <% if (adminCan('produktion') || adminCan('prototypen')) { %><span class="ad-nav__sep">Fertigung</span><% } %>
      <% if (adminCan('produktion')) { %><a href="/verwaltung/produktion" class="<%= currentPath.indexOf('/produktion') === 0 ? 'is-active' : '' %>"><svg aria-hidden="true"><use href="#i-tools"/></svg>Produktion</a><% } %>
      <% if (adminCan('prototypen')) { %><a href="/verwaltung/prototypen" class="<%= currentPath.indexOf('/prototypen') === 0 ? 'is-active' : '' %>"><svg aria-hidden="true"><use href="#i-star"/></svg>Prototypen</a><% } %>
      <% if (adminCan('einkauf')) { %><span class="ad-nav__sep">Einkauf</span>
      <a href="/verwaltung/lieferanten" class="<%= currentPath.indexOf('/lieferanten') === 0 ? 'is-active' : '' %>"><svg aria-hidden="true"><use href="#i-truck"/></svg>Lieferanten</a>
      <a href="/verwaltung/einkauf" class="<%= currentPath.indexOf('/einkauf') === 0 ? 'is-active' : '' %>"><svg aria-hidden="true"><use href="#i-cart"/></svg>Einkauf</a><% } %>
      <% if (adminCan('bestellungen.lesen') || adminCan('kunden.lesen') || adminCan('gutscheine') || adminCan('versandarten')) { %><span class="ad-nav__sep">Verkauf</span><% } %>
      <% if (adminCan('bestellungen.lesen')) { %><a href="/verwaltung/bestellungen" class="<%= currentPath.indexOf('/bestellungen') === 0 ? 'is-active' : '' %>"><svg aria-hidden="true"><use href="#i-cart"/></svg>Bestellungen</a><% } %>
      <% if (adminCan('kunden.lesen')) { %><a href="/verwaltung/kunden" class="<%= currentPath.indexOf('/kunden') === 0 ? 'is-active' : '' %>"><svg aria-hidden="true"><use href="#i-user"/></svg>Kunden</a><% } %>
      <% if (adminCan('belege')) { %><a href="/verwaltung/belege" class="<%= currentPath.indexOf('/belege') === 0 ? 'is-active' : '' %>"><svg aria-hidden="true"><use href="#i-shield"/></svg>Belege</a><% } %>
      <% if (adminCan('versand')) { %><a href="/verwaltung/versand" class="<%= currentPath.indexOf('/versand') === 0 && currentPath.indexOf('/versandarten') !== 0 ? 'is-active' : '' %>"><svg aria-hidden="true"><use href="#i-truck"/></svg>Versand</a><% } %>
      <% if (adminCan('gutscheine')) { %><a href="/verwaltung/gutscheine" class="<%= currentPath.indexOf('/gutscheine') === 0 ? 'is-active' : '' %>"><svg aria-hidden="true"><use href="#i-star"/></svg>Gutscheine</a><% } %>
      <% if (adminCan('versandarten')) { %><a href="/verwaltung/versandarten" class="<%= currentPath.indexOf('/versandarten') === 0 ? 'is-active' : '' %>"><svg aria-hidden="true"><use href="#i-truck"/></svg>Versandarten</a><% } %>
      <% if (adminCan('zahlungsarten')) { %><a href="/verwaltung/zahlungsarten" class="<%= currentPath.indexOf('/zahlungsarten') === 0 ? 'is-active' : '' %>"><svg aria-hidden="true"><use href="#i-lock"/></svg>Zahlungsarten</a><% } %>
      <% if (adminCan('haendler') || adminCan('gebiete.lesen') || adminCan('crm.berater') || adminCan('provision.eigene') || adminCan('fahrtenbuch')) { %><span class="ad-nav__sep">Vertrieb</span><% } %>
      <% if (adminCan('beratung')) { %><a href="/verwaltung/beratung" class="<%= currentPath.indexOf('/beratung') === 0 ? 'is-active' : '' %>"><svg aria-hidden="true"><use href="#i-leaf"/></svg>Produktberatung</a><% } %>
      <% if (adminCan('gebietsbuch')) { %><a href="/verwaltung/gebietsbuch" class="<%= currentPath.indexOf('/gebietsbuch') === 0 ? 'is-active' : '' %>"><svg aria-hidden="true"><use href="#i-package"/></svg>Gebietsbücher</a><% } %>
      <% if (adminCan('haendler')) { %><a href="/verwaltung/haendler" class="<%= currentPath.indexOf('/haendler') === 0 ? 'is-active' : '' %>"><svg aria-hidden="true"><use href="#i-user"/></svg>Händler</a><% } %>
      <% if (adminCan('gebiete.lesen')) { %><a href="/verwaltung/gebiete" class="<%= currentPath.indexOf('/gebiete') === 0 ? 'is-active' : '' %>"><svg aria-hidden="true"><use href="#i-shield"/></svg>Festgebiete</a><% } %>
      <% if (adminCan('crm.berater')) { %><a href="/verwaltung/berater" class="<%= currentPath.indexOf('/berater') === 0 ? 'is-active' : '' %>"><svg aria-hidden="true"><use href="#i-star"/></svg>Kundenberater</a><% } %>
      <% if (adminCan('provision.eigene')) { %><a href="/verwaltung/provision" class="<%= currentPath.indexOf('/provision') === 0 ? 'is-active' : '' %>"><svg aria-hidden="true"><use href="#i-star"/></svg><%= adminCan('provision.verwalten') ? 'Provision' : 'Meine Provision' %></a><% } %>
      <% if (adminCan('provision.eigene')) { %><a href="/verwaltung/verdienst" class="<%= currentPath.indexOf('/verdienst') === 0 ? 'is-active' : '' %>"><svg aria-hidden="true"><use href="#i-tools"/></svg>Verdienstrechner</a><% } %>
      <% if (adminCan('provision.eigene')) { %><a href="/verwaltung/rangliste" class="<%= currentPath.indexOf('/rangliste') === 0 ? 'is-active' : '' %>"><svg aria-hidden="true"><use href="#i-star"/></svg>Rangliste</a><% } %>
      <% if (adminCan('fahrtenbuch')) { %><a href="/verwaltung/fahrten" class="<%= currentPath.indexOf('/fahrten') === 0 || currentPath.indexOf('/fahrzeuge') === 0 ? 'is-active' : '' %>"><svg aria-hidden="true"><use href="#i-truck"/></svg>Fahrtenbuch</a><% } %>
      <% if (adminCan('rezepte') || adminCan('bewertungen') || adminCan('newsletter')) { %><span class="ad-nav__sep">Inhalte &amp; Marketing</span><% } %>
      <% if (adminCan('rezepte')) { %><a href="/verwaltung/rezepte" class="<%= currentPath.indexOf('/rezepte') === 0 ? 'is-active' : '' %>"><svg aria-hidden="true"><use href="#i-leaf"/></svg>Rezepte &amp; Ratgeber</a><% } %>
      <% if (adminCan('bewertungen')) { %><a href="/verwaltung/bewertungen" class="<%= currentPath.indexOf('/bewertungen') === 0 ? 'is-active' : '' %>"><svg aria-hidden="true"><use href="#i-star"/></svg>Bewertungen</a><% } %>
      <% if (adminCan('newsletter')) { %><a href="/verwaltung/newsletter" class="<%= currentPath.indexOf('/newsletter') === 0 ? 'is-active' : '' %>"><svg aria-hidden="true"><use href="#i-user"/></svg>Newsletter</a><% } %>
      <% if (adminCan('termine') || adminCan('nachrichten')) { %><span class="ad-nav__sep">Zusammenarbeit</span><% } %>
      <% if (adminCan('termine')) { %><a href="/verwaltung/termine" class="<%= currentPath.indexOf('/termine') === 0 ? 'is-active' : '' %>"><svg aria-hidden="true"><use href="#i-star"/></svg>Termine</a><% } %>
      <% if (adminCan('nachrichten')) { %><a href="/verwaltung/nachrichten" class="<%= currentPath.indexOf('/nachrichten') === 0 ? 'is-active' : '' %>"><svg aria-hidden="true"><use href="#i-user"/></svg>Nachrichten</a><% } %>
      <span class="ad-nav__sep">Konto</span>
      <% if (adminCan('einstellungen')) { %><a href="/verwaltung/einstellungen" class="<%= currentPath.indexOf('/einstellungen') === 0 ? 'is-active' : '' %>"><svg aria-hidden="true"><use href="#i-tools"/></svg>Einstellungen</a><% } %>
      <a href="/verwaltung/team" class="<%= currentPath.indexOf('/team') === 0 ? 'is-active' : '' %>"><svg aria-hidden="true"><use href="#i-lock"/></svg><%= adminCan('team') ? 'Zugänge' : 'Mein Konto' %></a>
      <% if (adminCan('protokoll')) { %><a href="/verwaltung/protokoll" class="<%= currentPath.indexOf('/protokoll') === 0 ? 'is-active' : '' %>"><svg aria-hidden="true"><use href="#i-shield"/></svg>Protokoll</a><% } %>
    </nav>
    <div class="ad-side__foot">
      Angemeldet als<br><strong style="color:#fff"><%= admin.name || admin.email %></strong><br>
      <a href="/" target="_blank" rel="noopener">Shop ansehen</a>
      <form method="post" action="/verwaltung/abmelden" style="margin-top:12px">
        <input type="hidden" name="_csrf" value="<%= csrfToken() %>">
        <button class="btn btn--ghost btn--sm btn--block" type="submit">Abmelden</button>
      </form>
    </div>
  </aside>

  <div class="ad-main">
    <header class="ad-top">
      <h1><%= title %></h1>
      <div class="ad-top__meta">
        <% if (typeof headActions !== 'undefined') { %><%- headActions %><% } %>
      </div>
    </header>
    <div class="ad-body">
      <% (flashes || []).forEach(function (f) { %>
        <div class="ad-alert ad-alert--<%= f.type %>"><%= f.message %></div>
      <% }); %>
